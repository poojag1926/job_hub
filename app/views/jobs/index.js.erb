<% if @jobs.empty? %>
	$('.job-list').html("<div class='alert alert-info'>No related jobs are present.</div>");
<% else %>
	$('.job-list').html("<%= escape_javascript render(partial: 'jobs/job_list', locals: {:@jobs => @jobs}) %>");
<% end %>


